// Stub implementation for non-web platforms (mobile/desktop)
import 'package:flutter/material.dart';

void toggleWebFullscreen(ValueNotifier<bool> isFullscreen) {
  // This function is not called on non-web platforms
  // Fullscreen is handled by SystemChrome instead
}
